name: java-workflow

on:
  push:
    branches: ["main"]

jobs: 
  build:
    name: my-spc
    runs-on: self-hosted

    steps: 
      - name: Git checkout
        uses: actions/checkout@v4
    
      - name: Build java with maven
        uses: mvn package