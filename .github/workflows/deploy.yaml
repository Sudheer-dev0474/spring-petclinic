name: ICARE-CICD-PRO

on:
  push:
    branches:
      - main
  
jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: setup java
        uses: actions/setup-java@v4
      - name: build project
        run: mvn clean package